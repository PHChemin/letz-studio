---
description: Stack e fluxo do esqueleto Letz Studio (landings GitHub Pages)
alwaysApply: true
---

# Letz Studio — regras do projeto

Este repositório é um **esqueleto** para landings freelance estáticas.

## Stack (não trocar sem pedido explícito)

- Vite + HTML + Tailwind CSS v4 (`@tailwindcss/vite`)
- Deploy: GitHub Pages via `.github/workflows/deploy.yml`
- Sem React/Next/backend neste template

## Ao personalizar um cliente

1. Ler `docs/CLIENT_BRIEF.md` e `docs/PRD.md` (preencher se vazios).
2. Seguir `docs/UI_GUIDANCE.md` e tokens em `src/styles/main.css`.
3. Trocar placeholders em `index.html` (nome, textos, links, imagens).
4. Contato só via links (`wa.me`, `mailto:`, redes) — Pages é estático.
5. Deploy e domínio: `docs/DEPLOY.md`.

## Design

- Hero full-bleed; marca forte no primeiro viewport.
- Evitar layouts genéricos “dashboard”, cards no hero, chips flutuantes.
- Evitar tipografia/cores clichê de IA (Inter, roxo genérico, cream+terracotta default).

## Build

- `npm run dev` / `npm run build` — o Pages consome apenas `dist/`.
- Não commitar `node_modules/` nem `dist/`.
